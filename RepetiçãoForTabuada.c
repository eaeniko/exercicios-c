#include <stdio.h>

 void main () {
int N, i;
		
		printf ("Digite um numero de 0 a 10: "); scanf ("%d",&N);
		
			for (  i = 0; i <=10; i++ ) {
				printf ("\n %d * %d = %d",N,i, N*i,);
				}
	
	system("PAUSE");	
}
